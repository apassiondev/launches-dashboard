{
  "name": "nasa-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm run dev:server & npm run dev:app",
    "dev:server": "npm run dev --prefix server",
    "dev:app": "npm start --prefix app",
    "test:app": "npm test --prefix app",
    "test:server": "npm test --prefix server",
    "test": "npm run test:app && npm run test:server",
    "install-server": "npm install --prefix server",
    "install-app": "npm install --prefix  app",
    "install": "npm run install-server && npm install-app",
    "deploy": "npm run build --prefix app && npm run start --prefix server",
    "deploy-cluster": "npm run build --prefix app && npm run cluster --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {}
}
